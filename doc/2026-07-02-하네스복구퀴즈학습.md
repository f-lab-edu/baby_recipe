# 2026-07-02 작업 기록

## 1. 학습 하네스 Windows 환경 복구

### 1-1. 문제 발견
- `/loop`를 이용해 하네스 상태를 점검하던 중, Stop 훅이 깨져있는 것을 발견.
- `.claude/settings.json`의 훅 명령이 WSL 경로(`/home/jy_sm/test/baby_recipe/...`)를 가리키고 있었는데, 현재는 Windows(`D:/claude/baby_recipe`)에서 작업 중이라 경로가 존재하지 않아 실행 실패.

### 1-2. 수정 내용
- `.claude/settings.json`: Stop 훅 커맨드 경로를 `D:/claude/baby_recipe/.claude/study/daily_question.sh`로 변경.
- `daily_question.sh`:
  - `python3`가 Windows 스토어 스텁일 경우 `python`으로 대체하는 감지 로직 추가.
  - Git Bash 경로(`/d/...`)를 Windows Python이 읽을 수 있도록 `cygpath -m`으로 변환.
  - cp949 인코딩 에러 해결 — `PYTHONUTF8=1` 환경변수 설정 + `open(..., encoding="utf-8")` 명시.
- 수정 후 스크립트 직접 실행해서 오늘의 질문(15/28)이 정상 출력되는 것 확인.
- 커밋: `6b38e35 fix: 학습 하네스 Windows 환경 이전에 따른 경로/인코딩 수정`

### 1-3. 커밋 메시지 스타일 논의
- 영어 커밋 스타일 예시를 보여줬으나, 사용자가 기존 한국어 + Conventional Commits(`fix:`/`feat:`) 스타일 유지를 선택.
- 이 프로젝트에서는 앞으로도 한국어 커밋 메시지를 기본으로 함 (메모리에 저장).

## 2. `/loop 공부` 퀴즈 세션 (JWT/보안, 도메인/JPA)

### 2-1. loop_state.json 신규 생성
- 기존에 `loop_state.json` 파일이 없어서 `current_index: 0`으로 새로 생성 후 시작.

### 2-2. 진행한 문제 (1~12번, 총 28문제 중)
| 번호 | 카테고리 | 주제 | 결과 |
|------|----------|------|------|
| 1 | JWT/보안 | OncePerRequestFilter 상속 이유 | 오답 → 해설 |
| 2 | JWT/보안 | recipes permitAll vs feed authenticated | 미흡(순환논리) → 해설 |
| 3 | JWT/보안 | JWT subject에 userId 저장 이유 | 정답 |
| 4 | JWT/보안 | RefreshToken Rotation | 오답(만료로 착각) → 해설 |
| 5 | JWT/보안 | 로그인 실패 통합 메시지 (User Enumeration 방지) | 힌트 후 정답 |
| 6 | JWT/보안 | SessionCreationPolicy.STATELESS | 힌트 후 정답 |
| 7 | 도메인/JPA | @NoArgsConstructor(PROTECTED) | 힌트 후 미흡 → 해설 |
| 8 | 도메인/JPA | @OrderBy("stepOrder ASC") | 오답(문법만 언급) → 해설 |
| 9 | 도메인/JPA | tags는 @ManyToMany, steps/ingredients는 @OneToMany | 힌트 후 정답 |
| 10 | 도메인/JPA | cascade ALL + orphanRemoval 조합 | 힌트 후 정답 |
| 11 | 도메인/JPA | User.followings/followers와 Follow 엔티티 | 건너뜀 (어렵다고 판단) |
| 12 | 도메인/JPA | @Enumerated(EnumType.STRING) | 세션 종료로 미진행 |

### 2-3. 진행 중 별도 학습
- 퀴즈 도중 사용자가 JWT, JPA 기본 개념을 모른다고 해서 별도로 설명 진행:
  - JWT vs 세션 방식 차이 (stateless 개념)
  - JWT가 웹 외에 모바일/마이크로서비스 인증에도 쓰인다는 점
  - JPA의 역할과 리플렉션 기반 객체 생성 원리

## 다음에 할 것
- `/loop 공부`로 재개하면 `loop_state.json`의 `current_index: 11` (12번 문제, `@Enumerated(EnumType.STRING)`)부터 이어서 진행됨.
- 11번 문제(`followings`/`followers`와 `Follow` 엔티티 관계)는 건너뛴 상태라 나중에 복습 필요.
