# Baby Food Tracker

## 프로젝트 개요
이유식 식단 기록 + 냉장고 재료 기반 레시피 추천 웹 서비스.
백엔드 포트폴리오 프로젝트 

## 기술 스택
- Java 
- Spring Boot 
- Spring MVC 
- MySQL

## /loop 공부 단축키

사용자가 `/loop 공부` 또는 `/loop 퀴즈`로 루프를 시작하면,
`.claude/study/loop_prompt.md` 파일을 읽고 지시에 따라 BabyRecipe 퀴즈를 진행한다.

---

## 세션 종료 (자동 저장 규칙)

사용자가 아래 중 하나를 말하면 **즉시** Write 도구로 doc 파일을 생성한다. 다른 어떤 작업보다 우선한다.

트리거 단어: `대화 종료`, `오늘 여기까지`, `마칠게`, `끝낼게`, `종료할게`

### 파일 경로 규칙
```
doc/YYYY-MM-DD-[주제 2~3단어].md
예) doc/2026-06-29-학습하네스구현.md
```
- YYYY-MM-DD는 오늘 날짜 (date +%Y-%m-%d 기준)
- 주제는 오늘 대화의 핵심을 한국어 2~3단어로 요약 (붙여쓰기)
- 같은 날 두 번째 파일은 `-2` 접미사 추가

### 저장 내용 형식
기존 doc 파일 형식을 따른다 (doc/2026-06-10.md 참조).

```markdown
# YYYY-MM-DD 작업 기록

## 1. [작업 주제]

### 1-1. [세부 항목]
- 무엇을 했는지 (What)
- 왜 그렇게 했는지 (Why) — 설계 결정, 트레이드오프
- 명령어/코드 스니펫 (있으면)

## 주요 변경 파일 (있으면)
| 파일 | 변경 내용 |
|------|-----------|
| ... | ... |

## 다음에 할 것 (있으면)
- ...
```

파일 저장 후 "📝 doc/파일명.md 저장 완료"를 사용자에게 알린다.